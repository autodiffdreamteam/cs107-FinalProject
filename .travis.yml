language: python

# safelist
branches:
  
  only:
  - master
  - milestone3_aditya


install:
  - pip install pytest-cov
  - pip install codecov
  - pip install -U pytest
  - pip install numpy


script: 
  - pytest --cov=./tests/
  
after_success:

  - codecov

